CREATE TABLE app_logs (
    asc_time DATE, 
    name VARCHAR(50),
    level_name VARCHAR(50),
    messages VARCHAR(250)
);

CREATE TABLE email_logs (
    asc_time DATE, 
    name VARCHAR(50),
    level_name VARCHAR(50),
    messages VARCHAR(250)
);

